# 第四章 解线性方程组的迭代法题单

> 覆盖 Jacobi、Gauss–Seidel、松弛迭代及其收敛性判定与谱半径分析的题目。

## 1. 向量与矩阵范数、谱半径及收敛性

- 强对角占优矩阵与迭代收敛  
  - `2016_A(无) 填空-7`：强对角占优矩阵的定义。  
  - `2007-08_B(无) 填空-7`、`2006_B(无) 填空-7`：参数 a 满足什么条件时，高斯–赛德尔迭代必收敛。  
  - `2017_A(docx) 填空-7`：系数矩阵为强对角占优矩阵时，高斯–赛德尔迭代必收敛。  

- 一般线性迭代 `x_{k+1}=Bx_k+g` 收敛 ⇔ 谱半径 ρ(B)<1  
  - `2006_A(无) 填空-8`、`2008_A(含) 填空-7`、`2008_B(无) 填空-3`、`2009_A(含) 填空-9`：统一考察谱半径条件。  

## 2. Jacobi、Gauss–Seidel 及松弛迭代

- 写出迭代矩阵与迭代公式并分析收敛性  
  - `北京理工大复习题及答案 第三章`：给定 5×5 方程组，分别写出 Jacobi 与 Gauss–Seidel 迭代算法并编程实现。  
  - `2007-08_B(无) 计算-3`：判断 Jacobi 与 Gauss–Seidel 的收敛性并采用收敛方法求解。  
  - `2008_A(含) 计算-5`：Gauss–Seidel 迭代解 3×3 方程组。  
  - `2009_A(含) 计算-3`、`2009_B(无) 计算-3`：将方程组改写为适合 Jacobi / Gauss–Seidel 的等价形式，并写出迭代公式。  
  - `2011_A(无) 计算-3`：给定 4×4 方程组，用 Gauss–Seidel 迭代求解到指定精度。  
  - `2013_A(无) 计算-3`：高斯–赛德尔迭代求 3×3 方程组解。  
  - `2016_A(无) 计算-4`：带松弛因子 ω=1.03 的逐次松弛法解 3×3 方程组。  
  - `2017_A(docx) 计算-3`：将方程组化为适合 Gauss–Seidel 的形式并进行迭代计算。  

- 松弛法与松弛因子 ω  
  - `2008_B(无) 填空-10`、`2008_A(含) 填空-10`：给出松弛法（ω=0.9）解 3 元方程组的迭代公式。  
  - `2013_A(无) 填空-10`、`2013_A(无) 填空-13`：逐次松弛法中对 ω 的发散条件与迭代表现。  


