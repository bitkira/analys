# 数值分析考试题目按章节整理（本科）

> 说明：  
> - 覆盖本目录下绝大多数本科《数值分析》试卷与复习题（PDF / DOCX）。  
> - 章节对应你给出的教学安排：误差 → 方程迭代 → 线性方程组直接法 → 线性方程组迭代法 → 插值法 → 数值积分（含相关扩展）。  
> - “考点”只概括主要知识点，不给出完整解答，方便按章节刷题。  
> - 题号记法：`[年份_卷别] 题型-题号`，例如 `2008_A(含) 填空-3` 表示 `2008级数值分析期末试题（A）（含答案）` 中的填空第 3 小题。  
> - 仅在我能从文件中可靠读出的题目上做了标注；扫描质量差或文字抽取失败的内容标为“未细化”，见文末“未完成部分说明”。

---

## 第一章 数值计算中的误差

### 1. 有效数字、绝对误差与相对误差

- 根据近似数的小数位判定有效数字位数、绝对误差限、相对误差限  
  - `2006_A(无) 填空-1`：`x=0.03600` 有效数字与误差限。  
  - `2006_B(无) 填空-2`：`a=3.201, b=0.57` 的乘加结果误差估计。  
  - `2007-08_B(无) 填空-2`：同样是 `a=3.201, b=0.57` 的误差估计。  
  - `2008_A(含) 填空-3`：`a=3.201, b=0.57` 结果的有效数字位数。  
  - `2008_B(无) 填空-8`：`...4.472135/20` 的相对误差控制。  
  - `2009_A(含) 填空-1`：`x≈0.231` 近似 `x*=0.229` 的有效数字判断。  
  - `2009_B(无) 填空-1`：`x1=721, x2=0.721, x3=0.700, x4=7×10⁻²` 有效数字。  
  - `2011_A(无) 填空-2`：`√20` 近似值的相对误差和有效数字。  
  - `2013_A(无) 填空-1`：`x1=1.21, x2=3.65, x3=9.81` 组合运算的相对误差限。  
  - `2016_A(无) 填空-1`：`a=0.025` 四舍五入后的有效数字及相对误差。  
  - `2017_A(docx) 填空-1`：梯形面积的绝对误差、相对误差、有效数字位数。

- 误差来源与总误差构成（模型误差、观测误差、截断误差、舍入误差）  
  - `2011_A(无) 填空-3`：总误差由哪两类误差构成及合理分配原则。  
  - `2006_B(无) 填空-1`：截断误差与舍入误差的最佳分配。  
  - `2009_A(含) 判断-6`：总误差中各类误差的构成。  

### 2. 舍入方法、误差在运算中的传播

- 运算结果有效数字与运算误差估计（加减、乘除）  
  - `2006_B(无) 填空-2`、`2007-08_B(无) 填空-2`、`2008_A(含) 填空-3`、`2009_A(含) 填空-2`（矩阵范数与向量范数伴随不等式）等。  

- 表达式重写以减小消去误差  
  - `2016_A(无) 填空-3`：将 `√(x+1)-√x` 改写成避免大数相减的形式。  
  - `2017_A(docx) 填空-5`：对“大正数 x 时”的某表达式改写以提高精度。  

- 误差分配与算法稳定性（牛顿—柯特斯系数为负时的稳定性、误差最佳分配原则）  
  - `2006_A(无) 填空-9`：`n≤?` 时牛顿–科特斯公式稳定。  
  - `2007-08_B(无) 填空-9`：同样关于牛顿–科特斯稳定性的讨论。  
  - `2009_A(含) 判断-9`：截断误差与舍入误差最佳分配原则。  

### 3. 泰勒展开与截断误差估计（作为误差分析工具）

- 用泰勒/Maclaurin 级数估计函数值与截断误差  
  - `2013_A(无) 填空-3`：用 Taylor 级数计算 `cos 1`，给出最后一项与每项有效数字。  
  - `2017_A(docx) 填空-2`：用 Maclaurin 展开式计算 `sin(1.0)`，根据截断误差≤0.01 决定项数。  

---

## 第二章 方程（组）的迭代解法

### 1. 对分法、简单迭代与迭代函数收敛性

- 对分法求根与迭代次数估计  
  - `2008_B(无) 填空-5`：`f(x)=1-x-sin x` 在 [0,1] 上用二分法，误差≤0.5×10⁻⁴ 所需对分次数。  
  - `2011_A(无) 填空-5`：`2x²-5x-1=0` 在 [1,3] 的根，两步对分后的区间。  
  - `2016_A(无) 填空-8`：`f(x)=0` 在 [0,1] 上用对分法，误差≤0.5×10⁻³ 的对分次数。  
  - `2013_A(无) 填空-4`：对分法用于选取迭代初值的误差限。  

- 迭代函数的收敛判据（|φ'(x)|<1、谱半径等价）  
  - `2008_A(含) 填空-7`：一般线性迭代 `x_{k+1}=Mx_k+N` 的收敛充要条件。  
  - `2008_B(无) 填空-3`：同样 `x_{k+1}=Mx_k+N` 收敛条件。  
  - `2009_A(含) 填空-3` / `2009_A(含) 填空-17`：|φ'(x)|<1 的局部收敛性条件。  
  - `2009_B(无) 填空-3`：线性迭代 `x_{k+1}=Bx_k+g` 收敛的谱半径条件。  
  - `2011_A(无) 填空-9`：迭代公式 `X_{k+1}=MX_k+N` 收敛充要条件。  
  - `2013_A(无) 填空-6`：为使某线性迭代收敛，对参数 a 的条件。  
  - `北京理工大复习题.pdf 第三章`：关于 Jacobi / Gauss–Seidel 收敛性的编程题。  

### 2. 牛顿法、牛顿下山法、弦截法、埃特肯加速

- 基本牛顿迭代公式与局部二阶收敛性质  
  - `2007-08_B(无) 判断-2`、`2009_A(含) 判断-2`：牛顿法在单根附近平方收敛的判断。  
  - `2009_A(含) 计算-6`：从 `f(x)=0` 导出求 a 的迭代公式（用于开方）。  
  - `2008_A(含) 计算-2`、`2009_A(含) 计算-6`、`复习题.pdf 第一章` 中多处 Newton 迭代求根/求 a。  

- 牛顿下山法与下山条件  
  - `2007-08_B(无) 填空-4`、`2008_A(含) 填空-11`、`2008_B(无) 填空-9`：给出 `x_{k+1}=x_k-λ f(x_k)/f'(x_k)` 的下山条件。  
  - `2011_A(无) 填空-6`：针对 `x²-2=0` 的牛顿下山步骤与下山因子。  
  - `2017_A(docx) 填空-3`：`x³-2x-5=0` 在 [0,4] 上，已算出 `x₁=7`，求合适的下山因子及新的 `x₁`。  

- 弦截法（双点、单点）  
  - `2006_A(无) 填空-4`：双点弦截法的收敛条件。  
  - `2006_B(无) 填空-3`：`x⁴-4x²-4=0` 在 [1,2] 的双点弦截迭代公式。  
  - `2008_A(含) 填空-1`、`2008_B(无) 填空-1`：双点弦截法收敛条件的判定。  
  - `2017_A(docx) 计算-1`：单点弦截法求某方程在 [1,2] 内的根。  

- 埃特肯 Δ² 加速法  
  - `2016_A(无) 填空-4`：`x0, y1=φ(x0), z1=φ(y1)` 已给，写出 Aitken 加速后的 `x1`。  
  - `2013_A(无) 填空-4`：对 `f(x)=x³-x²-1` 先用对分选初值，再用埃特肯法一次加速。  

### 3. 迭代格式的收敛性比较与误差估计

- 把方程改写成不同的迭代格式并比较收敛性  
  - `2008_A(含) 选择-2`、`2011_A(无) 计算-1`：给出三种 φ(x)，判断在某区间附近的收敛性并选择。  
  - `2013_A(无) 填空-4`、`2011_A(无) 计算-1`：针对三种格式选取收敛的并计算根。  

- 迭代步数或误差估计  
  - `2017_A(docx) 填空-4`：给定初值与误差要求，估计需要迭代步数。  

---

## 第三章 解线性方程组的直接法

### 1. 高斯消元法及其变形（克劳特、平方根、追赶）

- 高斯消元基础与可解条件  
  - `2008_A(含) 选择-3`：线性方程组能用高斯消元法求解的充要条件。  
  - `2009_A(含) 判断-1`：非奇异矩阵方程组一定可用高斯消元的判断。  
  - `2007-08_B(无) 填空-13`：消元法由“消元过程”和“回代过程”两部分组成。  

- 选主元高斯消元、列主元素法  
  - `2008_A(含) 选择-4`、`2011_A(无) 选择-3`：选主元的目的是减小舍入误差。  
  - `2007-08_B(无) 选择-3`、`2009_B(无) 选择-3`：第一步消元主元素的选择。  
  - `2017_A(docx) 填空-6`：给出线性方程组，问第一次消元时选哪个主元。  
  - 典型计算题：  
    - `2008_A(含) 计算-4`：高斯消元解 3×3 方程组。  
    - `2009_A(含) 计算-1`、`2011_A(无) 计算-2`、`2013_A(无) 计算-5`：列主元高斯消元求解。  

- 克劳特消元、平方根法、追赶法  
  - `2017_A(docx) 计算-2`：平方根法解对称正定方程组。  
  - `2016_A(无) 计算-3`：克劳特消元法解给定 `AX=B`。  
  - `北京理工大复习题.pdf 第二章`：编程实现追赶法，并求梯形电阻网络电流。  

### 2. 矩阵分解与平方根法适用条件

- 平方根法适用的系数矩阵条件（对称正定）  
  - `2013_A(无) 填空-9`：平方根法解方程组的条件。  
  - `2016_A(无) 填空-9`：同类判断题。  

### 3. 线性方程组与矩阵范数

- 向量、矩阵范数计算与不等式  
  - `2008_A(含) 填空-5`、`2008_B(无) 填空-7`：给定 2×2 矩阵求 ‖A‖₁ 或 ‖A‖∞。  
  - `2009_A(含) 填空-2`、`2009_A(含) 填空-16`：矩阵与向量的 ∞-范数以及 ‖AX‖∞ 的估计。  
  - `2011_A(无) 填空-7,8`：向量 1-范数、矩阵 ∞-范数与 2-范数。  
  - `2013_A(无) 填空-7,8`、`2016_A(无) 填空-12,14`、`2017_A(docx) 填空-8,9`：向量 2-范数及矩阵 1/∞/2-范数。  

---

## 第四章 解线性方程组的迭代法

### 1. 向量与矩阵范数、谱半径及收敛性

- 强对角占优矩阵与迭代收敛  
  - `2016_A(无) 填空-7`：强对角占优的定义。  
  - `2007-08_B(无) 填空-7`、`2006_B(无) 填空-7`：参数 a 满足某条件时，高斯–赛德尔迭代必收敛。  
  - `2017_A(docx) 填空-7`：系数矩阵为“强对角占优矩阵”时，高斯–赛德尔必收敛。  

- 一般线性迭代 `x_{k+1}=Bx_k+g` 收敛 ⇔ ρ(B)<1  
  - `2006_A(无) 填空-8`、`2008_A(含) 填空-7`、`2008_B(无) 填空-3`、`2009_A(含) 填空-9`。  

### 2. Jacobi、Gauss–Seidel 及松弛迭代

- 写出迭代矩阵和迭代公式  
  - `北京理工大复习题.pdf 第三章`：给定 5×5 方程组，分别写出 Jacobi 与 Gauss–Seidel 迭代算法并编程。  
  - `2007-08_B(无) 计算-3`：判断 Jacobi 与 Gauss–Seidel 收敛性并用收敛的那种迭代求解。  
  - `2008_A(含) 计算-5`：高斯–赛德尔迭代解 3×3 方程组。  
  - `2009_A(含) 计算-3`、`2009_B(无) 计算-3`：将方程组改写成迭代易收敛的形式，并写出 Jacobi 与 Gauss–Seidel 公式。  
  - `2011_A(无) 计算-3`：给定 4×4 方程组，写 GS 迭代公式并求解到给定精度。  
  - `2013_A(无) 计算-3`：高斯–赛德尔迭代求 3×3 方程组解。  
  - `2016_A(无) 计算-4`：带松弛因子 ω=1.03 的逐次松弛法解 3×3 方程组。  
  - `2017_A(docx) 计算-3`：将方程组改成适合 Gauss–Seidel 的形式并计算。  
  - `2008_B(无) 填空-10`、`2008_A(含) 填空-10`：给出松弛法（ω=0.9）的迭代公式。  
  - `2013_A(无) 填空-10`、`北京理工 2013_A 填空-13`：逐次松弛法中对 ω 的发散条件。  

---

## 第五章 插值法

### 1. 差商与牛顿插值

- 差商定义与高阶差商特性  
  - `2006_A(无) 填空-3`、`2006_B(无) 填空-5`：多项式 `f(x)=a_n x^{n+1}+…` 的 n 阶差商。  
  - `2007-08_B(无) 填空-14`、`2011_A(无) 填空-4,14`：差商与多项式次数关系。  
  - `2009_A(含) 填空-4,18`、`2009_B(无) 填空-2`：`f(x)=x⁷-x³+1` 或类似多项式的高阶差商。  
  - `2016_A(无) 填空-9,15`：给定差商值求某点的高阶差分。  

- 牛顿插值公式（不等距节点）  
  - `2008_A(含) 填空-4`：给出 3 个节点函数值，求拉格朗日多项式（亦可视为牛顿）。  
  - `2009_B(无) 填空-6`：已知节点 (-1,3),(1,1),(2,-1) 的二次牛顿基本差商公式。  
  - `2013_A(无) 计算-2`：斯特林插值公式（等距节点牛顿插值的对称形式）。  
  - `复习题.pdf 第五章`：编程实现 Newton 差值并观察龙格现象。  

### 2. 拉格朗日插值、多项式唯一性

- 一维拉格朗日插值  
  - `2006_A(无) 填空-11`、`2007-08_B(无) 填空-11`：三点给定值，构造拉格朗日多项式。  
  - `2008_A(含) 填空-4`：给出三点函数值，写出 Lagrange 多项式。  
  - `2009_A(含) 计算-4` / `2009_B(无) 计算-6`：给定若干点，用 Lagrange 插值后做反插值。  
  - `2016_A(无) 计算-5`：用两点插值求 `sin(0.33)`。  
  - `2017_A(docx) 计算-4`：利用插值多项式做反插值求 `x-e^{-x}=0` 的解。  

- 插值多项式唯一性  
  - `2008_A(含) 选择-5`、`2009_A(含) 判断-5`：节点相同、插值条件相同 ⇒ 插值多项式唯一。  

### 3. 等距节点牛顿插值与差分表

- 前向/后向/中心差分公式的选取与舍入误差估计  
  - `2009_A(含) 填空-6`、`2009_A(含) 填空-20`：首节点 / 尾节点附近选用前插、后插或中心差分。  
  - `2013_A(无) 填空-12`：填写差商表、构造插值多项式所需差商。  

### 4. 反插值问题

- 用插值多项式做反求自变量  
  - `2008_A(含) 计算-1`：已知单调函数若干点，用插值法求 `f(x)=0.5` 的 x。  
  - `2009_A(含) 计算-5`、`2013_A(无) 计算-5`：给数据表，求使 `f(x)=1` 等条件的自变量值。  
  - `2017_A(docx) 计算-4`：利用反插值求 `x-e^{-x}=0` 的根。  

### 5. 埃尔米特插值与样条插值

- Hermite 插值（函数值+导数或高阶导数）  
  - `2008_A(含) 计算-6`、`2009_A(含) 计算-2`：构造 3 阶或 4 阶 Hermite 多项式并写截断误差。  
  - `2011_A(无) 计算-5`、`2013_A(无) 计算-6`、`2016_A(无) 计算-6`：给 `f(x), f'(x)`（甚至 `f''(x)`）数据，构造 Hermite 插值。  
  - `北京理工大 2007-08_B(无) 计算-4`：含导数条件的三次插值多项式。  

- 三次样条函数与边界条件（自然、固定）  
  - `2008_A(含) 填空-9`、`2009_A(含) 填空-5,19`：三次样条在每段是 3 次多项式且在区间上具有二阶连续导数。  
  - `2011_A(无) 填空-12`：自然边界条件 `S''(a)=S''(b)=0`。  

---

## 第六章 数值微分和数值积分（含扩展）

### 1. 牛顿–柯特斯公式与代数精确度

- 插值型求积公式的代数精度  
  - `2006_A(无) 填空-5`、`2008_A(含) 填空-2`、`2008_B(无) 填空-1`：n 个节点的插值型公式至少 n−1 次，高斯公式 2n−1 次。  
  - `2017_A(docx) 填空-11`：同上。  

- Newton–Cotes 系数与公式阶数  
  - `2006_A(无) 填空-9`、`2011_A(无) 填空-13`：n=4 时的牛顿—柯特斯系数 C₀,C₁,C₂,C₃。  
  - `2008_A(含) 填空-8`：n=3 时的 Newton–Cotes 系数。  
  - `2009_A(含) 填空-8`、`2009_B(无) 填空-11`：n=4 时系数的延伸。  
  - `2016_A(无) 填空-11,13`：某求积公式的代数精度、辛卜生公式的代数精度。  

### 2. 复化梯形/辛卜生公式与误差估计

- 在给定误差要求下确定分段数  
  - `2016_A(无) 填空-10`：Integral `∫₀¹ eˣ dx`，复化梯形/辛卜生分段数的估计。  
  - `2008_A(含) 计算-8`、`2008_B(无) 计算-8`、`2011_A(无) 计算-6`：对 `∫₀¹ 1/(1+x) dx` 等，要求截断误差≤10⁻² 时的分段数并计算近似值。  
  - `2013_A(无) 填空-5`：`∫₀¹ eˣ dx` 用复化梯形公式，误差≤0.5×10⁻⁵ 时分段数。  

- 直接应用复化梯形 / 复化辛卜生公式  
  - `2011_A(无) 计算-6`：复化辛卜生求 `∫₀¹ sin x dx` 并估计舍入误差（给出函数表）。  
  - `2017_A(docx) 计算-6`：给定 n=8 节点表，用复合梯形与复合辛卜生计算积分。  
  - `2009_B(无) 计算-5`、`北京理工 2009-2010_B(无) 计算-5`：给定函数表，用复化辛卜生求积分。  

### 3. 龙贝格积分与自适应加速

- Romberg 步骤公式  
  - `2008_A(含) 填空-8`：T₁,S₁ 已知，求 R₁。  
  - `2016_A(无) 计算-7`：龙贝格求积计算 `∫₀¹ (1/(1+2x²)) dx`。  
  - `2017_A(docx) 填空-13`：已知 T₁=4,S₁=5，求下一层 R。  

### 4. 高斯求积公式

- n 节点高斯公式、给定节点与权重计算积分  
  - `2008_A(含) 计算-3`、`2008_B(无) 计算-2`：三点高斯公式求 `∫_{-1}^1 1/(1+5x) dx`。  
  - `2016_A(无) 填空-11,47`：三阶代数精度的高斯公式需要节点数。  
  - `2017_A(docx) 填空-14`：n=2 高斯公式求某给定积分。  

### 5. 扩展：常微分方程的数值解（课程后续章节）

> 虽然不在你给出的 1–6 章提纲中，但多份试卷最后一题涉及 ODE 初值问题的 Euler 法、改进 Euler、梯形法，可视作课程后续章节或复习内容。

- Euler 法、隐式 Euler 法、梯形法  
  - `2008_A(含) 计算-7`、`2009_B(无) 计算-8`：对 `y' = f(x,y)` 初值问题，用 Euler / 改进 Euler 计算到 x=0.5。  
  - `2007-08_B(无) 计算-6`：Euler、隐式 Euler、梯形法三种格式的比较。  
  - `2011_A(无) 计算-7`、`2013_A(无) 计算-4`：给定微分方程，用 Euler 类方法求数值解。  
  - `IMG_20191206_173449.jpg`：照片中第 7 题同样是 Euler 与改进 Euler。  

---

## 其他：数据拟合、特征值问题等

> 这些内容在《复习题及答案》与部分试卷中出现，一般归入课程后面章节（最小二乘、特征值问题），这里单独列出，供扩展复习。

- 最小二乘法数据拟合  
  - `北京理工大复习题及答案.pdf 第六章-2`：将 (x,y) 数据变换后做线性回归，求 a,b。  

- 幂法与反幂法（求特征值）  
  - `北京理工大复习题及答案.pdf 第四章-2`：幂法 3 次迭代得到近似特征值，再用反幂法精化。  

---

## 未完成部分说明

- `含答案/2017解析版.pdf`：该文件以特殊编码内嵌整页图片，PyPDF2 只能解出乱码（`/UniGB-UTF16-H not implemented`），难以自动提取题目文字；由于 `2017 A试卷.docx` 中已经有完整 A 卷题目，解析版仅提供解析，本次未逐题整理。  
- `含答案/数值分析-试题及答案.pdf`：文本抽取结果长度不足 30 字符，基本为空白，推测为扫描占位或损坏的 PDF，未能获得题目内容。  
- `含答案/IMG_20191206_173449.jpg` 与 `IMG_20191206_173458.jpg`：为拍照版试卷/答案，已人工粗略浏览，确认主要包含：插值（Hermite）、龙贝格积分、Gauss 求积、线性方程组迭代等考点，与上文各章考点高度重合。由于图片分辨率与排版限制，本次未做到“逐小题编号精确对应”，只在相关章节中将其作为整体示例引用。  
- 以上之外的 PDF 试卷（2006–2016 年 A/B 卷及 2008/2009 含答案 A 卷、2009–2010 B 卷、复习题集）中的题目都已至少按“章节 + 考点”层面归类，并给出对应试卷名与题号。个别填空题若只涉及简单代数计算或与周围题目完全同一考点，未重复罗列；不影响按章节刷题的覆盖度。

---

如果你希望更细致到“每一份卷子的每一空/每一问都列出”，我们可以在这份基础上继续细化，把某一年的 A/B 卷单独展开成子表。现在这份 `exam_topic_map.md` 可以作为章节复习和查找题源的总索引使用。

