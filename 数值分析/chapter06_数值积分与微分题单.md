# 第六章 数值微分和数值积分题单

> 包括牛顿–柯特斯公式、复化梯形/辛卜生、龙贝格、高斯求积以及 ODE 数值解（扩展）。

## 1. 牛顿–柯特斯公式与代数精确度

- 插值型求积公式的代数精度  
  - `2006_A(无) 填空-5`、`2008_A(含) 填空-2`、`2008_B(无) 填空-1`：n 个求积节点的插值型公式至少 n−1 次，高斯公式 2n−1 次。  
  - `2017_A(docx) 填空-11`：同样考代数精度。  

- Newton–Cotes 系数与公式阶数  
  - `2006_A(无) 填空-9`、`2011_A(无) 填空-13`：n=4 的牛顿–柯特斯系数 C₀,C₁,C₂,C₃。  
  - `2008_A(含) 填空-8`：n=3 的 Newton–Cotes 系数。  
  - `2009_A(含) 填空-8`、`2009_B(无) 填空-11`：n=4 时的系数与代数精度。  
  - `2016_A(无) 填空-11,13`：某求积公式代数精度、辛卜生公式的代数精度。  

## 2. 复化梯形/辛卜生公式与误差估计

- 在给定误差要求下确定分段数  
  - `2016_A(无) 填空-10`：`∫₀¹ eˣ dx`，分别用复化梯形与复化辛卜生公式，求满足给定误差的分段数。  
  - `2008_A(含) 计算-8`、`2008_B(无) 计算-8`、`2011_A(无) 计算-6`：对 `∫₀¹ 1/(1+x) dx` 等，要求截断误差≤10⁻² 时的分段数并计算近似值。  
  - `2013_A(无) 填空-5`：`∫₀¹ eˣ dx` 用复化梯形公式，误差≤0.5×10⁻⁵ 时所需分段数。  

- 直接应用复化梯形 / 复化辛卜生公式  
  - `2011_A(无) 计算-6`：复化辛卜生求 `∫₀¹ sin x dx` 并估计舍入误差（给出函数表）。  
  - `2017_A(docx) 计算-6`：给 n=8 节点表，用复合梯形与复合辛卜生计算积分值。  
  - `2009_B(无) 计算-5`、`北京理工 2009-2010_B(无) 计算-5`：给定函数表，用复化辛卜生计算定积分。  

## 3. 龙贝格积分与自适应加速

- Romberg 步骤公式及应用  
  - `2008_A(含) 填空-8`：给定 T₁,S₁，计算 R₁。  
  - `2016_A(无) 计算-7`：龙贝格积分法计算 `∫₀¹ 1/(1+2x²) dx`。  
  - `2017_A(docx) 填空-13`：已知 T₁=4, S₁=5，求下一层 R。  

## 4. 高斯求积公式

- n 节点高斯公式、给定节点与权重计算积分  
  - `2008_A(含) 计算-3`、`2008_B(无) 计算-2`：三点高斯公式求 `∫_{-1}^1 1/(1+5x) dx`。  
  - `2016_A(无) 填空-11,47`：三阶代数精度的高斯求积公式所需节点数。  
  - `2017_A(docx) 填空-14`：n=2 高斯公式计算给定积分。  

## 5. 扩展：常微分方程的数值解

- Euler 法、隐式 Euler 法、梯形法求 ODE 初值问题  
  - `2008_A(含) 计算-7`、`2009_B(无) 计算-8`：对 `y' = f(x,y)` 初值问题，用 Euler / 改进 Euler 计算到 x=0.5。  
  - `2007-08_B(无) 计算-6`：Euler、隐式 Euler、梯形法三种格式的比较与误差分析。  
  - `2011_A(无) 计算-7`、`2013_A(无) 计算-4`：给定微分方程，用 Euler 类方法求解。  
  - `IMG_20191206_173449.jpg`：照片中第 7 题，同样为 Euler 与改进 Euler 的计算题。  

