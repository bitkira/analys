# 第二章 方程（组）的迭代解法题单

> 按“对分法 / 牛顿类 / 弦截法 / Aitken 加速 / 收敛性比较”等子模块整理。

## 1. 对分法、简单迭代与迭代函数收敛性

- 对分法求根与迭代次数估计  
  - `2008_B(无) 填空-5`：`f(x)=1-x-sin x` 在 [0,1] 上用二分法，误差≤0.5×10⁻⁴ 所需对分次数。  
  - `2011_A(无) 填空-5`：`2x²-5x-1=0` 在 [1,3] 的根，两步对分后的区间。  
  - `2016_A(无) 填空-8`：`f(x)=0` 在 [0,1] 上用对分法，误差≤0.5×10⁻³ 的对分次数。  
  - `2013_A(无) 填空-4`：对分法用于选取迭代初值的误差限。  

- 迭代函数的收敛判据（|φ'(x)|<1、谱半径等价）  
  - `2008_A(含) 填空-7`：一般线性迭代 `x_{k+1}=Mx_k+N` 的收敛充要条件。  
  - `2008_B(无) 填空-3`：同样 `x_{k+1}=Mx_k+N` 的收敛条件。  
  - `2009_A(含) 填空-3` / `2009_A(含) 填空-17`：|φ'(x)|<1 的局部收敛性条件。  
  - `2009_B(无) 填空-3`：线性迭代 `x_{k+1}=Bx_k+g` 收敛的谱半径条件。  
  - `2011_A(无) 填空-9`：迭代公式 `X_{k+1}=MX_k+N` 收敛充要条件。  
  - `2013_A(无) 填空-6`：为使某线性迭代收敛，对参数 a 的条件。  
  - `北京理工大复习题及答案.pdf 第三章`：关于 Jacobi / Gauss–Seidel 收敛性的编程题（附矩阵谱半径分析）。  

## 2. 牛顿法、牛顿下山法、弦截法、埃特肯加速

- 基本牛顿迭代公式与局部二阶收敛性质  
  - `2007-08_B(无) 判断-2`、`2009_A(含) 判断-2`：牛顿法在单根附近平方收敛的判断。  
  - `2009_A(含) 计算-6`：从 `f(x)=0` 导出求 a 的迭代公式（如开 n 次方）。  
  - `2008_A(含) 计算-2`、`复习题及答案 第一章`：Newton 迭代求方程根或参数。  

- 牛顿下山法与下山条件  
  - `2007-08_B(无) 填空-4`、`2008_A(含) 填空-11`、`2008_B(无) 填空-9`：`x_{k+1}=x_k-λ f(x_k)/f'(x_k)` 的下山条件。  
  - `2011_A(无) 填空-6`：针对 `x²-2=0`，给出不满足下山条件的步长并调整 λ。  
  - `2017_A(docx) 填空-3`：`x³-2x-5=0` 在 [0,4] 上，`x₁=7` 不满足下山条件，求合适 λ 与新的 `x₁`。  

- 弦截法（双点、单点）  
  - `2006_A(无) 填空-4`：双点弦截法收敛条件。  
  - `2006_B(无) 填空-3`：`x⁴-4x²-4=0` 在 [1,2] 的双点弦截迭代公式。  
  - `2008_A(含) 填空-1`、`2008_B(无) 填空-1`：双点弦截法收敛条件的判定。  
  - `2017_A(docx) 计算-1`：单点弦截法求指定方程在 [1,2] 内的根（3 位有效数字）。  

- 埃特肯 Δ² 加速法  
  - `2016_A(无) 填空-4`：已知 `x0, y1=φ(x0), z1=φ(y1)`，写出 Aitken 加速后的 `x1`。  
  - `2013_A(无) 填空-4`：对 `f(x)=x³-x²-1`，先用对分选初值，再用埃特肯法一次加速。  

## 3. 迭代格式的收敛性比较与误差估计

- 不同迭代格式的收敛性比较  
  - `2008_A(含) 选择-2`、`2011_A(无) 计算-1`：给出三种 φ(x)，判断在某区间附近的收敛性并选择。  
  - `2011_A(无) 计算-1`：在 `x₀=1.5` 附近比较三种迭代格式的收敛性。  

- 迭代步数或误差估计  
  - `2017_A(docx) 填空-4`：给定初值与误差要求，估计迭代次数。  


